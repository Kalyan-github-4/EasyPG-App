// Use the native (WebView-based) implementation for all platforms
// WebView works reliably on both native and web in Expo Go
import PropertyLocationMap from "./PropertyLocationMap.native";

export default PropertyLocationMap;